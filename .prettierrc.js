module.exports = {
    // Line length
    printWidth: 100,
    
    // Use 2 spaces for indentation
    tabWidth: 2,
    
    // Use spaces (not tabs)
    useTabs: false,
    
    // Use semicolons
    semi: true,
    
    // Use single quotes
    singleQuote: true,
    
    // Object props are only quoted when needed
    quoteProps: 'as-needed',
    
    // Use single quotes in JSX
    jsxSingleQuote: false,
    
    // No trailing commas
    trailingComma: 'none',
    
    // Always use spaces inside brackets
    bracketSpacing: true,
    
    // Place closing brackets on the same line
    bracketSameLine: false,
    
    // Always include parentheses around arrow function parameters
    arrowParens: 'avoid',
    
    // Preserve existing line endings
    endOfLine: 'lf',
    
    // Format embedded content
    embeddedLanguageFormatting: 'auto'
  };